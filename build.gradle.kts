// kombin/build.gradle.kts (Root Level)
plugins {
    id("com.android.application") version "8.2.0" apply false // Sürüm sende farklı olabilir
    id("org.jetbrains.kotlin.android") version "1.9.0" apply false // 'apply false' BURADA ÇOK KRİTİK
}